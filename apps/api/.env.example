# Environment
SGP_PLUS_ENV=des

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/sgp_plus_des

# Cookie Settings
COOKIE_NAME=sgp_plus_session
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=
COOKIE_PATH=/
SESSION_TTL_MINUTES=720

# CORS (lista separada por vírgula; não usar * com cookie auth)
CORS_ORIGINS=http://localhost:5173

# Bootstrap Admin (para seed) — obrigatório trocar a senha
BOOTSTRAP_ADMIN_EMAIL=admin@sgp.local
BOOTSTRAP_ADMIN_PASSWORD=CHANGE_ME
